- name: TornadoServer
  matchers:
  - part: headers.server
    type: regex
    name: version
    regex:
        - TornadoServer\/([\d.]+)
- name: jupyter
  matchers:
    - type: word
      part: title
      words:
        - Jupyter Notebook