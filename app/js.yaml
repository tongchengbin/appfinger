#各种前端组件
- matchers:
    - group: 0
      name: version
      part: body
      regex:
        - jquery-([0-9\.]+)(\.min)?\.js
      type: regex
  name: JQuery
- matchers:
    - part: body
      regex:
        - bootstrap.(min\.)css
      type: regex
    - part: body
      group: 0
      name: version
      regex:
        - bootstrap.min.js?ver=([\d\.]+)
      type: regex
  name: bootstrap
- matchers:
    - part: body
      regex:
        - <meta [^>]*name="DC\.title"[^>]*>
      type: regex
  name: dublincore
- name: iconify-icon
  matchers:
    - part: body
      type: word
      words:
        - iconify-icon.min.js
- matchers:
    - part: headers.vary
      type: word
      words:
        - x-inertia
  name: inertia.js
- matchers:
    - group: 1
      name: version
      part: headers.x-powered-by
      regex:
        - ^next\.js ?([0-9.]+)?
      type: regex
  name: next.js
- matchers:
    - part: body
      words:
        - ext-base.js
      type: word
  name: ext.js